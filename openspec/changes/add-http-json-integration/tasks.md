## 1. B0 Catalog pins + vitest (1 writer)

- [x] 1.1 Exclusive: `pnpm-workspace.yaml` catalog keys `"ipaddr.js": "2.5.0"` (no-op if already present), `"ssrfcheck": "1.4.0"`, `"@jmespath-community/jmespath": "1.3.0"`. Then `packages/integrations/package.json` deps catalog `ipaddr.js` + `ssrfcheck` and `workspace:*` core (keep octokit). `packages/plugins/package.json` deps workspace core + renderer + integrations plus catalog jmespath-community. Align plugins tsconfig with core `allowImportingTsExtensions`; keep `.js` specifiers. Copy/confirm `vitest.config.ts` under `packages/integrations`, `packages/plugins`, `packages/renderer` (same as core). Run `pnpm install` once after catalog edit. FORBIDDEN: extra undici, p-retry, msw, classic jmespath.

## 2. B1 Core schema gate (serial; exclusive `types.ts`)

- [x] 2.1 Exclusive: `packages/core/src/types.ts` only. **Read first.** If `rss`/`feed` or `wakatime`/`coding` already exist, **append** `http`/`json`/`http`; never replace arrays with `["github","http"]`. Add `FIRST_PARTY_PLUGIN_IDS += "http"`, `FIRST_PARTY_WIDGET_IDS += "json"`, `FIRST_PARTY_INTEGRATION_IDS += "http"`, `INTEGRATION_AUTH.http = "optional"`, `WIDGET_INTEGRATIONS.json = ["http"]`. Freeze comment: post-v0 exceptions are whatever ids are in the array.
- [x] 2.2 In the same `types.ts` write: `JSON_FILENAME_DEFAULT`, `JSON_JMESPATH_DEFAULT = "@"`, `JSON_TIMEOUT_MS_DEFAULT = 10000`, `JSON_TIMEOUT_MS_MAX = 20000`, `JSON_TIMEOUT_MS_MIN = 1`, `JSON_METHOD = "GET"`, `JSON_ANIMATE_DEFAULT = false`, `HTTP_RESPONSE_MAX_BYTES = 1048576`; `JsonOptionsSchema` strictObject with url `z.url({ protocol: /^https$/ })` required, headers optional record + superrefine, `JSON_OPTION_DEFAULTS` without url; `HttpWidgetsConfigSchema` / `HttpPluginConfigSchema`; `PluginsConfigSchema` optional `http`; `ActionInputsSchema.http_token_env` optional string. MUST NOT add `plugin_http` bool.

## 3. B2 early (parallel with B1; disjoint globs)

- [x] 3.1 B2d exclusive: `packages/integrations/src/cache.ts` + test. Run-scoped Map + single-flight. `restCacheKey({ method, url, params, auth, headers })` with `auth: "none" | "bearer"` (not the secret) and canonicalized yaml headers. `params` = sorted `URL.searchParams`. Do not add GraphQL keys. Do not touch `rss/cache.ts`.
- [x] 3.2 B2e exclusive: `packages/integrations/src/http/ssrf.ts` + test. `assertSafeHttpUrl` + `assertSafeResolvedAddresses`. ssrfcheck then ipaddr allow-unicast after IPv4-mapped unwrap. Mock `lookup`. Cases: `http://`, localhost, `127.0.0.1`, `10.0.0.1`, `172.16.0.1`, `192.168.1.1`, `169.254.169.254`, `::1`, `::ffff:10.0.0.1`, DNS→private, mixed public A + private AAAA (fail), userinfo host, decimal IP, `metadata.google.internal`, public example.com+public A pass. **192.168.0.0/16 not /8.**
- [x] 3.3 B2f exclusive: `packages/renderer/src/index.ts` (create or **extend**), `themes.ts`, `svg.ts` optional. `renderSvg(node, { width: 480, height: 160 })`. Register Geist WOFF2 (no `googleFonts`). Re-export `container`/`text`/`style`. Shared `renderSvg` uses `images: []` (Takumi 2.9.2 remote-image block; `{ allowUrl: false }` is not a valid option). Do **not** change `packages/renderer/src/render-svg.ts`. Dark/light palettes for seven tokens. If file exists (rss stub), add exports without breaking `renderSvg`. Test: SVG has 480 and 160; no `<style>` `@keyframes` SMIL `foreignObject`.
- [x] 3.4 B2h exclusive: `packages/integrations/src/http/headers.ts`, `auth.ts` + tests. Forbidden yaml headers; Bearer vs raw scheme; never log values.
- [x] 3.5 B2i exclusive: `packages/core/src/redact.ts` + test. Strip Authorization values and env secrets from error strings. Barrel `export * from "./redact.js"` via `packages/core/src/index.ts`.

## 4. B2 late (after B1)

- [x] 4.1 B2a exclusive: `packages/core/src/parse-config.ts`, `parse-config.test.ts`, `config.ts` only if needed. No http pack defaults. Tests: valid json yaml; missing url; `http://`; unknown key; `plugin_http` yaml key fails; Authorization header fails; **PLAN_DEFAULT_YAML still equals DEFAULT_CONFIG**; github `plugin_github` precedence unchanged.
- [x] 4.2 B2b exclusive: `packages/core/src/auth-policy.ts`, `auth-policy.test.ts`. `classifyHttp`, `decideHttpTokenEnv`, `usesHttpIntegration`, `decideHttpOnlyRunFailed`. GitHub classifiers **bit-identical**. `usesGithubIntegration` stays github-only (json must not join).
- [x] 4.3 B2c exclusive: `packages/core/src/codegen/action-yml.ts`, `flatten.ts` (`BANNED_FLATTENED_INPUT_NAMES` += `plugin_http_json_url`), generate-action tests, `just generate-action`. `http_token_env` thin input, no default. Schema keys === generated names. No `plugin_http` input.
- [x] 4.4 B2g exclusive: AGENTS.md ×6 after types exist (or draft github+http and include rss/wakatime if already mentioned): root, core, integrations, plugins, action, docs. Http exception; never-unauth GitHub unchanged; docs fixtures only. MUST NOT edit repo `README.md`.

## 5. B3 Client, widget, registry

- [x] 5.1 B3a exclusive: `packages/integrations/src/http/client.ts` + test + barrel `packages/integrations/src/index.ts` (merge if rss already exported). `createHttpClient({ fetch, lookup, token })`. Order: token decision → cache/single-flight → ssrf → GET (`redirect: "manual"`) → hops → size cap → BOM-strip JSON.parse → classifyHttp → backoff. `User-Agent: profile-bits-http/0`. `Accept: application/json`. Timeout `AbortSignal.timeout(timeout_ms)`. Http module MUST NOT import octokit.
- [x] 5.2 B3b exclusive: `packages/plugins/src/http/widgets/json/{index,template,jmes}.ts`. `search` from jmespath-community. fetch none (`vi.spyOn(globalThis, "fetch")` unused). Visual lock. Import renderer only. Empty → “No data”; invalid jmespath → `fail_widget`; `0`/`false` render; 480×160.
- [x] 5.3 B3c exclusive: `packages/plugins/src/http/plugin.ts`, `packages/plugins/src/index.ts`. id `http`, widgets `[json]`, integrations `[http]`, `defaults.widgets: ["json"]` metadata only, `docsPath: "/playground/http"`, `bitsUsed` string literals Theme/Frame/Stack/Row/Text/Muted. Merge barrel if rss pack already exported. Do not create `packages/bits`.

## 6. B4 Tests (test files / docs token keys only)

- [x] 6.1 B4a client tests: 200 coalesce; 401/404 no retry; 403/429/5xx retry then fail_widget (fake timers); non-JSON; oversized; Content-Length lie; no Authorization when unset; Bearer vs raw; empty env fail_widget; secrets absent from errors; redirect to private fails. Mock fetch/lookup; zero live network.
- [x] 6.2 B4b widget tests: fixture object/primitive/array; empty No data; invalid jmespath fail_widget; `0`/`false` render; no fetch.
- [x] 6.3 B4c accept: parse yaml + injected fetch JSON → `renderSvg` 480×160.
- [x] 6.4 B4d `just generate-action --check`. No new flattened Action inputs.
- [x] 6.5 B4e exclusive: `apps/docs/src/preview/types.ts` + permalink tests: add `http_token_env`, `http_token` to `PREVIEW_TOKEN_QUERY_KEYS`. Do **not** add http to `PREVIEW_PLUGIN_IDS`.

## 7. B5 Verify (parent)

- [x] 7.1 Run `just lint && just test && just generate-action --check`. Fix failures on the owning glob. Do not commit. Never two writers on `types.ts`, `auth-policy.ts`, `action-yml.ts`, `action.yml`, `pnpm-workspace.yaml`, `pnpm-lock.yaml`, `packages/renderer/src/index.ts`, or `packages/integrations/src/index.ts`.

## 8. Action json wiring + residual tests (do not untick 1–7)

- [x] 8.1 Residual SSRF tests exclusive: `packages/integrations/src/http/ssrf.test.ts` (optional extra cases in `client.test.ts` if URL literals go through `fetchJson`). URL literal `https://[::ffff:127.0.0.1]/api.json`; DNS `::ffff:127.0.0.1` / hex `::ffff:7f00:1`; `100.64.0.1` (CGNAT); `metadata.internal`. Keep existing `::ffff:10.0.0.1` resolved-address case.
- [x] 8.2 Residual json empty-card assertion exclusive: `packages/plugins/src/http/widgets/json/index.test.ts` (and a tiny template unit test if cleaner). `jsonTemplate({ empty: true })` children include `NO_JSON_DATA`. Baked SVG: 480×160, **no** `<text>`, no CSS/SMIL/`foreignObject`. Do not `svg.toContain("No data")`.
- [x] 8.3 Action package wiring (deps only) exclusive: `packages/action/package.json`, `packages/action/tsconfig.json`. Workspace deps: `@profile-bits/integrations`, `@profile-bits/plugins`, `@profile-bits/renderer` (core already path-mapped). `paths` for those packages, matching core. Do not import Takumi in the Action. `pnpm install` once after this writer (lockfile exclusive).
- [x] 8.4 Engine policy exclusive: `packages/action/src/engine.ts` + `engine.test.ts`. Widen `EnabledWidget` with `{ id: "json"; options: JsonOptions }`. `enabledWidgets`: github widgets if present **and** `config.plugins.http?.widgets?.json` if present. **No** early `return []` when github is absent. `preflightWidget`: `json` (and existing `demo` / `feed` if present) → `"render"`; do not read `include_private` on json. After the widget loop: keep `decideAllGithubWidgetsSkipped`. Then `decideHttpOnlyRunFailed({ widgets: outcomes, allowSkipped })`. If `"fail_job"`, throw `EngineError` with `"fail_job"`. Throw `fail_widget` only when a **github** widget is `fail_widget`. Json `fail_widget` in a mixed run MUST still write successful github blobs. Engine MUST NOT import integrations/plugins or perform HTTP. Tests (injected `renderWidget` stubs, no network): http-only yaml + render → writes `json.svg` (or filename from options); http-only + json `fail_widget` + `allow_skipped: false` → `fail_job`; http-only + `allow_skipped: true` → no throw, no json file; github render stub + json `fail_widget` → github files written, job succeeds; all-github-skipped unchanged (`AllGithubWidgetsSkippedError`); json does not join github skip-all. FORBIDDEN: rss/wakatime wiring in this change; GitHub crawl/render. If `feed` is already in `EnabledWidget` from the sibling rss change, leave it.
- [x] 8.5 HTTP render adapter exclusive: new `packages/action/src/render-http.ts` + `render-http.test.ts`. `createHttpRenderWidget({ client: HttpClient }): RenderWidget`. On `id !== "json"`: do not catch github; return control to compositor (or throw a typed “unhandled id” only if called directly). On json: `renderJsonFromClient(client, { url, jmespath, timeout_ms, headers })` → `{ id, outcome: "render", files: [{ path: "<filename>.<ext>", contents: svg }] }`. Map `JsonWidgetError` / `HttpClientError` → `outcome: "fail_widget"` (never `fail_run`). Tests: injected `createHttpClient({ fetch, lookup })`; yaml headers hit GET; empty token client fails widget; 480×160 svg; zero live network. Adapter MUST NOT import `engine.ts`.
- [x] 8.6 Compositor exclusive: `packages/action/src/render-widgets.ts`. `composeRenderWidgets({ json, github? })`: json ids → json adapter; other ids → `github` or the existing “not injected” `EngineError`. Production `main` passes json only.
- [x] 8.7 `main.ts` injection exclusive: `packages/action/src/main.ts`; keep hoisted `runEngine` mock tests in `main.test.ts`; add **separate** `packages/action/src/main-http.test.ts` that does **not** mock engine. `runMain` looks up `env[inputs.http_token_env]` (raw env, not `INPUT_*`). `createHttpClient({ token })` with **no** `fetch` (pinned GET; never `fetch: globalThis.fetch`). Injectable `fetch`/`lookup` only in tests via optional `RunMainOptions` seam (`httpFetch?`, `httpLookup?`). `runEngine(loaded, { renderWidget: composeRenderWidgets({ json: createHttpRenderWidget({ client }) }) })`. One client instance per `runMain` call. `main-http.test.ts`: http-only yaml + memory fs + injected fetch/lookup → `files` includes json svg; named empty env → fail widget / `fail_job`; secrets absent from thrown messages. `github_token` still required (`decideActionToken`). FORBIDDEN: rss/wakatime clients; GitHub `createGithubClient` / stats/languages crawl.
- [x] 8.8 Docs + verify: `packages/action/AGENTS.md` documents json enablement, `http_token_env` lookup, engine port, pinned GET, http-only `fail_job`. Still no `plugin_http`. Focused vitest on `@profile-bits/core`, `@profile-bits/integrations`, `@profile-bits/plugins`, `@profile-bits/renderer`, `@profile-bits/action`. `pnpm generate-action --check`. Do not require green `just test` if `docs-playground` is still red from a sibling change. Do not commit. Do not change `packages/renderer/src/render-svg.ts`. Never two writers on `engine.ts`, `main.ts`, `action.yml`, `types.ts`, `pnpm-lock.yaml`, `packages/integrations/src/index.ts`, or `packages/renderer/src/render-svg.ts`.
