## 1. H1 Types, presets, normalize, fixtures, Chip

- [ ] 1.1 H1.types exclusive: `packages/core/src/types.ts` only. **Read first.** Append `FIRST_PARTY_WIDGET_IDS += "chips"` and `WIDGET_INTEGRATIONS.chips = ["http"]`. Do **not** add plugin or integration ids. Do **not** replace id arrays. Keep `json` / rss / wakatime. Add `HTTP_CHIP_PRESETS = ["shieldcn","shields"]`, `HTTP_CHIP_TYPES` closed enum (`npm`, `stars`, `forks`, `license`, `release`, `issues`, `prs`, `ci`), `CHIPS_FILENAME_DEFAULT = "chips"`, `CHIPS_TYPES_MAX = 8`, `CHIPS_WORKFLOW_DEFAULT = "ci.yml"`, `CHIPS_ANIMATE_DEFAULT = false`. `ChipsOptionsSchema` strictObject: `filename`, `preset`, `types` (min 1 max 8, superrefine unique / dedupe preserve order), optional `package`, `repo`, `workflow`, `timeout_ms` (reuse `JSON_TIMEOUT_MS_*`), `animate`. **No `url`. No `headers`. No `bits`.** `HttpWidgetsConfigSchema`: optional `chips` beside `json`. Export types from the core barrel if needed. FORBIDDEN: `plugin_http` bool; new first-party plugin/integration ids; editing `main.ts`; editing `openspec/changes/add-http-json-integration/`.
- [ ] 1.2 H1.presets exclusive new `packages/integrations/src/http/presets.ts` + `presets.test.ts`. `expandChipsRequest({ preset, type, user, repo, packageName, workflow })` → `{ url: URL }` https only. Path tables from `design.md` / integration-contract. Origin allowlist: `shieldcn.dev`, `img.shields.io` only (reject `www`, `shields.io` without `img`, `http://`). `repo` with `/` splits owner/repo; else owner = `user`. Missing owner/package for a type → typed error (caller maps to `fail_widget`). Unit table tests, **zero network**. FORBIDDEN: `/badge/dynamic/json`, `/https/{hostname}`, `/memo`, discord, reddit, nba, views; second HTTP client; octokit import.
- [ ] 1.3 H1.normalize exclusive new `packages/integrations/src/http/normalize.ts` + `colors.ts` + tests. `normalizeBadgeJson(unknown)` → `{ label, message, color?: string }`. Accept shieldcn `value` or shields `message` (`message = message ?? value`). Empty/missing message → throw. Ignore `link`/`href`. `resolveChipColor(color, accent)` named map `brightgreen|green|yellowgreen|yellow|orange|red|blue|lightgrey|success|important|critical|informational|inactive` plus `#rgb`/`#rrggbb`; else `accent`. Inline-object fixtures (react npm without color; shields with color). Zero network.
- [ ] 1.4 H1.fixtures exclusive new `packages/integrations/src/http/fixtures/chips/**`. One JSON file per preset×type (16 files) plus `index.ts` `chipFixture(preset, type)`. Hand-authored from live shapes (no CI capture). shieldcn npm **without** `color`; shields with `color`. Zero live GETs.
- [ ] 1.5 H1.chip exclusive: `packages/bits/src/Chip.tsx` + `packages/bits/src/index.test.ts`. Optional `label` + `message` + `messageColor` horizontal split. Children-only path **bit-identical** for stats/languages. `div`/`span` + tw/style only. fromJsx test for split + children. Do **not** change `Stat.tsx`. Do **not** wrap Stat for the split API.

## 2. H2 Parse, flatten, plugin, auth tests, AGENTS

- [ ] 2.1 H2.parse exclusive: `packages/core/src/parse-config.ts` + `parse-config.test.ts` (and `config.ts` only if needed). Parse `plugins.http.widgets.chips`. Empty types fail. Unknown preset/type fail. Extra keys (`url`, `headers`, `bits`, `method`) fail. `plugins.http: {}` still widget-less. Default yaml github-only. Both `json` and `chips` together parse. Omitted `package`/`repo`/`workflow` parse. PLAN_DEFAULT_YAML still equals DEFAULT_CONFIG. FORBIDDEN: pack defaults that auto-enable chips.
- [ ] 2.2 H2.flatten exclusive: `packages/core/src/codegen/flatten.ts` + generate-action tests. `BANNED_FLATTENED_INPUT_NAMES += "plugin_http_chips_preset"`. No new thin Action inputs. No `plugin_http`. `just generate-action --check`.
- [ ] 2.3 H2.plugin exclusive: `packages/plugins/src/http/plugin.ts` + `plugin.test.ts`. `widgets: ["json","chips"]`. `integrations: ["http"]`. `defaults.widgets` stays `["json"]` metadata only. `bitsUsed` += `Chip` (keep Theme/Frame/Stack/Row/Text/Muted). Do not create a new pack.
- [ ] 2.4 H2.authTests exclusive: `packages/core/src/auth-policy.test.ts` only. Do **not** edit `auth-policy.ts` unless a compile forces it (`usesHttpIntegration` is already `WIDGET_INTEGRATIONS.includes("http")`). Update “true only for json”: `usesHttpIntegration("chips") === true`. `decideHttpOnlyRunFailed` with chips-only fail → `fail_job`; chips fail + stats render → `render`.
- [ ] 2.5 H2.agents exclusive AGENTS.md ×6 (one writer): root, `packages/core/AGENTS.md`, `packages/plugins/AGENTS.md`, `packages/integrations/AGENTS.md`, `packages/action/AGENTS.md`, `apps/docs/AGENTS.md`. Document widget `chips` on pack `http`; closed presets; still no extra plugin/integration ids; fixtures-only playground; **do not** add `/playground/http` UI in this change. Integrations: origin allowlist + SSRF; no second client. Engine enumerates json + chips; do not edit `main.ts`. MUST NOT edit repo `README.md`. MUST NOT edit `openspec/changes/add-http-json-integration/` or `docs-playground`.

## 3. H3 Widget, engine, barrel

- [ ] 3.1 H3.widget exclusive new `packages/plugins/src/http/widgets/chips/{index,template,load,widget}.ts(x)`. `loadChipsPayloads(client, options, ctx)` uses injected client only (mirrors `renderJsonFromClient`). Template/widget: no `fetch`. fromJsx + `assertTakumiTree` + `renderSvg`. Frame + wrapping Row of split Chips. 480×160 baked still. One failure → `ChipsWidgetError` `outcome: fail_widget` (entire widget; no partial card). Empty successful list → “No data”. Missing message after `message ?? value` → fail_widget. Missing color → theme accent. Ignore link/href. Import bits Chip + renderer only. No `takumi-js` / `@takumi-rs/*` / octokit in widgets.
- [ ] 3.2 H3.engine exclusive: `packages/action/src/engine.ts` + `engine.test.ts`. `EnabledWidget` union += json + chips (keep json if already present; append chips; do **not** regress `feed`). `enabledWidgets` reads `config.plugins.http?.widgets?.json` and `.chips`. Http widgets skip `include_private` preflight (`render`). After loop: github `fail_widget` keeps current throw; http-only uses `decideHttpOnlyRunFailed`; mixed github-render + http-`fail_widget` MUST NOT throw. Engine MUST NOT import integrations/plugins or perform HTTP. Tests with injected `renderWidget` (no network): http-only chips fail + `allow_skipped: false` → `fail_job`; stats render + chips `fail_widget` → github files written, job succeeds; json+chips both enabled; chips does not join github skip-all. FORBIDDEN: editing `packages/action/src/main.ts`; GitHub crawl/render; rss/wakatime wiring beyond leaving existing `feed`.
- [ ] 3.3 H3.barrel exclusive: `packages/plugins/src/index.ts` after 3.1. Export chips render helpers additively. Do not clobber json/rss/wakatime/github exports.

## 4. H4 Tests

- [ ] 4.1 H4.a presets/normalize/color tests if not completed in H1: full path table; origin rejects; shieldcn npm `{label,value,link}` → message; shields `{label,message,color}`; missing message throws; unknown color → accent. Zero network.
- [ ] 4.2 H4.b widget tests exclusive under `packages/plugins/src/http/widgets/chips/`. Fixtures → SVG 480×160; no `<style>` / `@keyframes` / SMIL / `foreignObject`; `vi.spyOn(globalThis, "fetch")` unused in template; empty → No data; one type fail → entire widget fail_widget.
- [ ] 4.3 H4.c accept test: yaml chips + injected fetch map (fixture JSON by URL) → `renderSvg` 480×160. Copy the json accept-test pattern. Zero live network.
- [ ] 4.4 H4.d engine tests (if not done in 3.2): http-only chips fail → fail_job; stats render + chips fail → success with github blobs; json+chips both enabled; `allow_skipped: true` http-only non-render does not throw. Injected `renderWidget` only.
- [ ] 4.5 H4.e `just generate-action --check`. Assert generated `action.yml` has no `plugin_http`, no `plugin_http_chips_preset`, no new flattened inputs.

## 5. H5 Playground (deferred — not this change)

Playground explorer is a **follow-on** OpenSpec change `add-http-chips-playground`. Do **not** create that folder in this apply. Do **not** add `/playground/http` UI, live shieldcn/shields fetches, or `http` to `PREVIEW_PLUGIN_IDS`. Do **not** edit `openspec/changes/docs-playground/` or implement `apps/docs/app/playground/http/**` here. H2.agents may mention chips as fixtures-only.

- [ ] 5.1 Confirm this apply leaves playground deferred: no `add-http-chips-playground` folder, no `/playground/http` UI, no live vendor fetches, `PREVIEW_PLUGIN_IDS` unchanged. Track H5 as a later change after 3.1 exists and no writer holds `apps/docs/**`.

## 6. H6 Verify

- [ ] 6.1 After H4: `just lint && just test && just generate-action --check`. Fix failures on the owning glob only. Do not commit. Do not push. Never two writers on `types.ts`, `parse-config.ts`, `flatten.ts`, `plugin.ts`, `engine.ts`, `Chip.tsx`, `packages/plugins/src/index.ts`, `main.ts`, `action.yml`, or `pnpm-lock.yaml`.
- [ ] 6.2 Confirm FORBIDDEN files were not edited: `packages/action/src/main.ts`, `openspec/changes/add-http-json-integration/**`, `openspec/changes/docs-playground/**`. Confirm no new first-party plugin/integration ids beyond github, wakatime, rss, http.
