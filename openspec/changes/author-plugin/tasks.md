## 1. T320e plugin.json and validate.sh

- [x] 1.1 Write `agent-plugin/profile-bits/plugin.json` for Agent Plugins 1.0.0: `$schema` const `https://agent-plugins.org/schemas/1.0.0/plugin.schema.json`, `name: profile-bits`, `version: 0.1.0`, `license: MIT`, keywords. MUST satisfy `additionalProperties: false`. MUST NOT add a `skills` array, MCP fields, or unknown top-level keys. MUST NOT create `mcp.json`.
- [x] 1.2 Write `agent-plugin/profile-bits/scripts/validate.sh`: validate `plugin.json` against 1.0.0 (vendor `references/plugin.schema.json` if needed so the script stays offline); fail if `mcp.json` is present; run `pnpm dlx skills-ref validate` on each `skills/<id>` once those dirs exist (name MUST match directory); if `justfile` has `generate-action`, run `just generate-action --check` from the repo root and fail closed on stale or flattened `plugin_*_*_*` inputs; fail if any template path contains `../`. Walking up to the repo root at runtime is allowed and is not a template `../`. MUST NOT add a just recipe. FORBIDDEN: `packages/**`, `apps/**`, `justfile`, `action.yml`, `biome.json`, other OpenSpec change folders.

## 2. T320a author router

- [x] 2.1 Write `agent-plugin/profile-bits/skills/author/SKILL.md` (`name: author`, `license: MIT`). Router MUST read OpenSpec `plugin-contract` / `widget-contract` / `integration-contract` (and `author-plugin` when present) plus `packages/core/src/types.ts`. New data source → `author-integration` first; new card → `author-widget` on an existing pack unless the author asked for a new pack; new pack → `author-plugin`. MUST NOT invent Action input names. Public API changes MUST require an OpenSpec delta first. When codegen recipes exist, tell the author to run `just generate-action` and `just generate-docs`. FORBIDDEN: other skill directories, `packages/**`, harness copies.
- [x] 2.2 Write `skills/author/evals/evals.json` covering **routing** for all seven cases: WakaTime integration; languages option; new pack using github+static; CSS animation for gif/apng; drop-in `widget.mdx` with no `source`; Tailwind stylesheet widget; exclusive `md.families.code` swap. Umbrella evals are routing only.

## 3. T320b author-integration

- [x] 3.1 Write `agent-plugin/profile-bits/skills/author-integration/SKILL.md` (`name: author-integration`) to generate into `packages/integrations/<id>/` (client, auth, scopes, inputs, mocked HTTP tests). Auth per integration and per widget option. `static` = none; `github` = token required in the Action. Shared client per run. Cache keys: REST `(method, url, params)`, GraphQL `(query, variables)`. A WakaTime-class add is an integration on this architecture, not a first-party pack and not a new fetch/auth stack.
- [x] 3.2 Add contained templates under `skills/author-integration/assets/templates/` (`client.ts.template`, `auth.ts.template`, `scopes.ts.template`, `inputs.ts.template`, `client.test.ts.template`). Destination paths live in SKILL.md as repo-root paths. MUST NOT use `../` in template paths. MUST NOT write live `packages/integrations/**`.
- [x] 3.3 Write `skills/author-integration/evals/evals.json` with the WakaTime integration case (new integration only; no first-party `wakatime` pack; mocked HTTP tests).

## 4. T320c author-widget

- [x] 4.1 Write `agent-plugin/profile-bits/skills/author-widget/SKILL.md` (`name: author-widget`): schema, fetch (no HTTP — consume cached payload), optional `source` (prefer omit; canonical `widget.tsx` | `widget.md` | `widget.mdx` | `widget.html`), bits-based Takumi-safe template, yaml options only (OpenSpec delta first; never flattened Action inputs). Encode `discoverSource` (extension → MIME → sniff; `.md` with import/export/JSX promotes to mdx; explicit `source` must match bytes or fail; ambiguous `widget.md` + `widget.tsx` without `source` fails). CSS `@keyframes` are authoring input to `renderAnimation`; APNG named `.png`; default SVG stays a baked still. Exclusive `md.families` (do not stack).
- [x] 4.2 Add contained templates (React, MD, MDX, stylesheet `tw`/`className`, CSS `@keyframes` for gif/apng) under `skills/author-widget/assets/templates/` with `.template` suffixes. MUST NOT use `../`. MUST NOT write live `packages/plugins/**`.
- [x] 4.3 Write `skills/author-widget/evals/evals.json` for: add a languages option (OpenSpec delta / yaml schema, no flattened Action input); widget with CSS animation (gif/apng, APNG `.png`, baked SVG still); drop-in `widget.mdx` with no `source`; Takumi-safe `tw`/`className` on div/span/img only; typed bit props; bits composition; exclusive `md.families.code` swap to starry-night (do not stack pretty-code).

## 5. T320d author-plugin pack skill

- [x] 5.1 Write `agent-plugin/profile-bits/skills/author-plugin/SKILL.md` (`name: author-plugin`): pack registry, defaults, `docsPath`, derived integration union. v0 MUST NOT add first-party ids to `FIRST_PARTY_PLUGIN_IDS`. A second pack is an OpenSpec + types change, not a silent catalog add. `demo` stays opt-in; github pack defaults remain `stats` + `languages`.
- [x] 5.2 Add contained pack-registry templates under `skills/author-plugin/assets/templates/` (no `../`). MUST NOT write live `packages/plugins/**`.
- [x] 5.3 Write `skills/author-plugin/evals/evals.json` for a new pack using github+static (derived union; OpenSpec before expanding plugin ids).

## 6. T321 harness symlinks

- [x] 6.1 Create relative symlinks (do not copy, do not hand-edit a second tree): `.agents/skills/author` → `../../agent-plugin/profile-bits/skills/author`; same for `author-integration`, `author-widget`, `author-plugin`. Leave generated `.agents/skills/openspec-*` untouched. MUST NOT write `.cursor/skills/` for these ids. Follow-up 7.1 retargets these links to `.agents/profile-bits` and MUST NOT create `.claude/` or `.claude/skills`.
- [x] 6.2 Write `agent-plugin/profile-bits/references/contract.md` pointing at `openspec/specs/plugin-contract`, `widget-contract`, `integration-contract`, and `packages/core/src/types.ts`. Note that `openspec/specs/author-plugin/` appears after T035 sync. Optional plugin-local `AGENTS.md` documenting `npx skills add ./agent-plugin/profile-bits --all`. FORBIDDEN: other change folders, `marketplace-release`, `packages/**` implementation, apply/sync/archive in the propose workflow, git commit.

## 7. Follow-up: SSOT migrate, dest, bitsUsed, catalog, ideate

Supersedes dest/catalog/root/projection language in §1–6. Do not uncheck applied T320/T321. Do not create `.claude/` or `.claude/skills`. Do not write `packages/**`. Do not sync, apply, or archive from this task list.

- [x] 7.1 SSOT migrate: move `agent-plugin/profile-bits` to `.agents/profile-bits` (real files). Delete leftover `agent-plugin/` (no alias, no copy). Retarget `.agents/skills/author`, `author-integration`, `author-widget`, and `author-plugin` to relative `../profile-bits/skills/<id>`. Leave `.agents/skills/openspec-*` and `.openspec-target` untouched. MUST NOT write `.cursor/skills/`. MUST NOT create `.claude/` or `.claude/skills`. Install path: `npx skills add ./.agents/profile-bits --all`.
- [x] 7.2 Dest `src/`: document and template destinations as `packages/integrations/src/<id>/` (client, auth, scopes, inputs, `client.test.ts`) and `packages/plugins/src/<pack>/widgets/<id>/` (pack registry `packages/plugins/src/<pack>/plugin.ts`). Barrel `packages/integrations/src/index.ts` mention-only. MUST NOT use `../` in templates. MUST NOT add a new `index.ts.template`. `docsPath` MUST be `{{DOCS_PATH}}` (MUST NOT hardcode `/generate/<id>/`).
- [x] 7.3 Pack-level `bitsUsed`: export `{{id}}Plugin` and `{{ID}}_BITS_USED`; put `bitsUsed` on the pack object (`satisfies PluginIdentity & { bitsUsed: typeof … }`); widget skill unions bits into that pack array. MUST NOT put `bitsUsed` on a widget yaml entry. MUST NOT add `widget-bits.ts` in core. MUST NOT edit `packages/core`. Drop `export const plugin`.
- [x] 7.4 Portable-core frontmatter on all four skills: `name`, `description`, `license: MIT`, `compatibility`, `metadata.author: profile-bits`, `metadata.version: "0.1.0"`. Compatibility: `Requires the profile-bits repo; OpenSpec plugin-contract, widget-contract, integration-contract, and author-plugin specs; packages/core/src/types.ts.` No Cursor-only keys.
- [x] 7.5 Catalog: skills MUST read `packages/core/src/types.ts` (`FIRST_PARTY_PLUGIN_IDS`, `FIRST_PARTY_WIDGET_IDS`, `FIRST_PARTY_INTEGRATION_IDS`, `WIDGET_INTEGRATIONS`, `INTEGRATION_AUTH`, `ActionInputsSchema` including `wakatime_token` / `http_token_env`). Completing an id already in those lists is allowed. Adding a new id requires OpenSpec first. MUST NOT silently expand `FIRST_PARTY_*`. MUST NOT create a second pack for an id already in `FIRST_PARTY_PLUGIN_IDS`. Replace github-only / never-wakatime-pack copy. WakaTime-class client shape (client, auth, scopes, inputs, mocked HTTP) MUST stay unchanged.
- [x] 7.6 Ideate on umbrella `author` only: modes `ideate` / `next` / `brainstorm` rank one next kind from live catalogs plus on-disk holes and MUST NOT write files. Empty-args MUST show a gallery (ideate as item 0) and stop — MUST NOT inventory. Four skills only. No `author-bit`. Named kind MUST be honored unless a lock fires. Engine JSON remains a subcommand flag: `pnpm exec openspec status --change author-plugin --json` (never `openspec --json`).

## 8. Follow-up: install freeze, empty-args skip, validation pin, this-spec sync

Supersedes §7 “do not sync” for the `author-plugin` spec only. §1–7 stay historically `[x]` (including 7.1 `--all`). Do not uncheck 1–7. Do not archive. Do not `openspec apply`. Do not MODIFIED `plugin-contract`, `widget-contract`, or `integration-contract`. Do not expand the locked-evals table. Engine JSON remains a subcommand flag (never `openspec --json`).

- [ ] 8.1 Retitle delta MODIFIED catalog heading to RENAMED TO (`Catalog SSOT is live FIRST_PARTY_* with no silent expand`); keep `## RENAMED` pairs.
- [ ] 8.2 Freeze install to `npx skills add ./.agents/profile-bits` (human command; MUST NOT `--all`; MUST NOT `-a claude-code` / `.claude/`; agents MUST NOT run `skills add` here). Soften Decision 1: three-layer specs own pack/yaml; default yaml MAY stay github-only; this change MUST NOT MODIFIED those capabilities.
- [ ] 8.3 Four skills: empty-args and help skip Before routing (gallery items 0–3; MUST NOT read `types.ts` or contracts).
- [ ] 8.4 Complete-existing no-clobber; widget bits import `@profile-bits/bits` (typed props; starter Theme, Frame, Stack, Row, Text, Muted; frozen 11 membership allow-list); placeholder dialects (pack `{{PLUGIN_ID}}`/`{{PLUGIN_ID_UPPER}}`, widget `{{PACK_ID}}`/`{{WIDGET_*}}`, integration `{{id}}`/`{{Id}}`/`{{ID}}`/`{{auth}}`/`{{scheme}}`).
- [ ] 8.5 Copy Plugin validation suite into the delta as MODIFIED: pin `pnpm dlx skills-ref@0.1.5`, no `agentskills`; Node `fs.realpathSync` containment; `plugin.json` identity (`name` `profile-bits`, version `0.1.0`, license `MIT`); recursive `mcp.json`.
- [ ] 8.6 MUST NOT add a new integration `index.ts.template`; pack barrel `index.ts.template` MUST NOT ship (live packs have no per-pack `index.ts`); integrations barrel mention-only.
- [ ] 8.7 Agent-merge this capability into `openspec/specs/author-plugin/spec.md` only (preserve main-only requirements). Do not archive. Do not MODIFIED three-layer contracts. Do not expand the locked-evals table.
- [ ] 8.8 `pnpm exec openspec validate author-plugin --type spec --strict` and `pnpm exec openspec validate author-plugin --type change --strict` MUST exit 0. Never `openspec --json`.
