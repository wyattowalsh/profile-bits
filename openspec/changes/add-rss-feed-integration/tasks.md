## 1. W0 OpenSpec artifacts (sequential; exclusive `openspec/changes/add-rss-feed-integration/**`)

- [x] 1.1 Confirm change `add-rss-feed-integration` exists with `.openspec.yaml` schema `spec-driven`. Exclusive: `openspec/changes/add-rss-feed-integration/**`. FORBIDDEN: `packages/**`, `apps/**` source, plan file, git commit.
- [x] 1.2 Confirm `proposal.md` lists MODIFIED `plugin-contract`, `widget-contract`, `integration-contract` (no new capability) and yaml-only rss enablement with no `plugin_rss`.
- [x] 1.3 Confirm spec deltas: MODIFIED plugin catalog + yaml shape + flattened-input check; ADDED rss pack / feed widget / rss integration (SSRF, GitHub-host fail, `fail_widget` after RSS backoff, frozen payload, rss-parser or equivalent).
- [x] 1.4 Confirm `design.md` locks rowanmanning parser, ipaddr.js DNS-pin SSRF, `fail_widget` after RSS backoff, fixture XML, no `plugin_rss`, `types.ts` exception, exclusive file paths.
- [x] 1.5 Confirm this `tasks.md` encodes the W0–W4 graph with exclusive globs. Tick 1.1–1.5 when artifacts exist; do not rewrite them during apply.

## 2. W1 Core schema gate (after W0; single writer)

- [x] 2.1 Exclusive: `packages/core/src/types.ts` only. Add `FIRST_PARTY_PLUGIN_IDS += "rss"`, `FIRST_PARTY_WIDGET_IDS += "feed"`, `FIRST_PARTY_INTEGRATION_IDS += "rss"`, `INTEGRATION_AUTH.rss = "none"`, `WIDGET_INTEGRATIONS.feed = ["rss"]`. Update the “Do not add more plugin ids” comment to “github + rss (post-v0)”.
- [x] 2.2 In the same `types.ts` write: `FEED_FILENAME_DEFAULT`, `FEED_LIMIT_MIN/MAX/DEFAULT` (1/8/5), `FEED_ANIMATE_DEFAULT`; `FeedOptionsSchema = z.strictObject({ filename, url: z.url({ protocol: /^https$/ }), limit, animate })` (protocol regex without colon); `RssWidgetsConfigSchema` / `RssPluginConfigSchema`; `PluginsConfigSchema` optional `rss`. MUST NOT add `plugin_rss` to `ActionInputsSchema`. MUST NOT edit `action-yml.ts`.

## 3. W2k Catalog pins + vitest (after W0; first in wave if install needed)

- [x] 3.1 Exclusive: `pnpm-workspace.yaml` catalog keys `"@rowanmanning/feed-parser": "2.1.4"` and `"ipaddr.js": "2.5.0"`. Then `packages/integrations/package.json` deps those two (keep octokit). `packages/plugins/package.json` deps `@profile-bits/core`, `@profile-bits/integrations`, `@profile-bits/renderer`. Copy `vitest.config.ts` under `packages/integrations`, `packages/plugins`, `packages/renderer` (same as core). Run `pnpm install` once after catalog edit (not parallel with other lockfile writes). FORBIDDEN: a second undici pin, msw, nock, rss-parser.

## 4. W2 parallel leafs (after W0; disjoint globs; launch together; do not wait on W1)

- [x] 4.1 W2c exclusive: `packages/integrations/src/rss/hosts.ts` + `hosts.test.ts`. GitHub suffix matcher; tests for `github.com`, `api.github.com.`, `gist.github.com`, `raw.githubusercontent.com`, `gitlab.com` (allow), case fold.
- [x] 4.2 W2d exclusive: `packages/integrations/src/rss/ssrf.ts` + `ssrf.test.ts`. Injectable `lookup` + `fetch`; ipaddr.js allow-only-unicast after IPv4-mapped conversion; https-only; `redirect: "manual"`; hop limit 5; 1 MiB bytes read; 10s timeout; DNS pin + TLS `servername` = original host. Tests: `http://`, `https://127.0.0.1/`, `https://169.254.169.254/`, `https://[::1]/`, mapped v6, private hostname via mocked DNS, redirect to private, redirect to http, oversize body, timeout.
- [x] 4.3 W2e exclusive: `packages/integrations/src/rss/cache.ts` + `cache.test.ts`. Key `(method, url, params)`; in-flight coalescing; run-scoped Map.
- [x] 4.4 W2f exclusive: `packages/integrations/src/rss/parse.ts` + `parse.test.ts`. `parseFeed` from `@rowanmanning/feed-parser`; map `published ?? updated` → ISO `published_at`; freeze items; `maxItems` slice is caller-side. Invalid XML → typed `RssParseError`. MUST NOT call parser HTTP.
- [x] 4.5 W2g exclusive: `packages/integrations/src/rss/outcomes.ts`. Status → `fail_widget` / retry (3 attempts, 200/400/800ms, `Retry-After` capped at 10s). MUST NOT import github `classifyGithubHttp`. 401/403/404 → `fail_widget`; 429/5xx → retry then `fail_widget`.
- [x] 4.6 W2h exclusive: `packages/integrations/src/rss/fixtures/atom.xml`, `rss2.xml`, `empty.xml`, `malformed.xml`, `xxe.xml` + `loadFixture.ts`. Atom and RSS 2.0 with ≥8 items; empty channel/feed for “No feed items”; XXE `SYSTEM`/`file://` must not read disk.
- [x] 4.7 W2i exclusive: `packages/renderer/src/index.ts` (and `svg.ts` if split). `renderSvg(node)` only; register vendored Geist from `packages/renderer/fonts/`; 480×160. T100 may replace later. MUST NOT implement gif/apng. Plugins MUST NOT import Takumi.

## 5. W2 after W1 (parse-config, flatten, AGENTS.md)

- [x] 5.1 W2a after W1. Exclusive: `packages/core/src/parse-config.test.ts` and `packages/core/src/config.ts` only if needed. MUST NOT change `DEFAULT_YAML`. Tests: rss yaml round-trip; missing url; `http://`; unknown key; github default yaml still equal `DEFAULT_CONFIG`; rss + github together; `plugin_github` still ignored when yaml present.
- [x] 5.2 W2b after W1. Exclusive: `packages/core/src/codegen/generate-action.test.ts` only. `--check` rejects `plugin_rss_feed_url`. Snapshot of thin `action.yml` still has `plugin_github` and **not** `plugin_rss`. MUST NOT add a `plugin_rss` input. MUST NOT edit `flatten.ts` / `action.yml` unless `--check` already covers the name (it matches `plugin_<plugin>_<widget>_<option>`).
- [x] 5.3 W2j after W1; parallel files. Exclusive: `packages/core/AGENTS.md` (schema freeze exception: rss/feed/rss integration ids); `packages/integrations/AGENTS.md` (add rss; fixtures wrap rss XML, not a second static pack); `packages/plugins/AGENTS.md` (packs `github` + `rss`; allow core edit **only** for rss schema; layout `src/rss/widgets/feed/`); `apps/docs/AGENTS.md` (playground rss uses fixture XML, no live feeds). MUST NOT edit repo `README.md`.

## 6. W3 Compose (after leafs)

- [x] 6.1 W3a after W2c–W2h. Exclusive: `packages/integrations/src/rss/client.ts` + `client.test.ts` + `packages/integrations/src/index.ts`. `createRssClient({ fetch, lookup })` one instance per run. Order: github-host → cache/single-flight → ssrf GET → parse → freeze. Client returns full frozen list; widget slices to limit. Headers: `User-Agent: profile-bits-rss/0`; `Accept: application/atom+xml, application/rss+xml, application/xml, text/xml;q=0.9, */*;q=0.1`. Never `Authorization`. Never parser HTTP. Never octokit.
- [x] 6.2 W3b after W1 + W2i + W3a. Exclusive tree: `packages/plugins/src/rss/**` + `packages/plugins/src/index.ts`. `plugin.ts`: id `rss`, widgets `[feed]`, integrations `[rss]`, `bitsUsed: ["Theme","Frame","Stack","Row","Text","Muted"]` as string literals (do not create `packages/bits`). `widgets/feed/template.ts`: Takumi node `width/height 100%`, card 480×160 tokens, titles up to `limit`, empty copy “No feed items”. `widgets/feed/index.ts`: fetch via rss client, slice to `limit`, no HTTP. Imports `./x.js`.

## 7. W4 Tests and verify (after W2a, W2b, W2j, W3a, W3b)

- [x] 7.1 Mocked HTTP/DNS tests (no live network): Atom + RSS 2.0 fixtures show up to `limit` titles on 480×160; `renderSvg` has no `<style>` / `@keyframes` / SMIL / `foreignObject`; empty fixture → “No feed items”; `limit: 1` and `limit: 8`; 401/403/404 → fail widget; 429/5xx retried then fail widget (not fail_job); malformed + xxe → fail widget; `http://` → parse fail; private IP / DNS-mocked private / GitHub.com → fail widget; two `createRssClient` calls on one instance → one fetch; github `DEFAULT_YAML` / `plugin_github` / thin `action.yml` snapshot unchanged aside from optional `plugins.rss`.
- [x] 7.2 Run `just lint && just test`. Do not commit. Never two writers on `types.ts`, `client.ts`, `plugin.ts`, `pnpm-lock.yaml`, or `action.yml`.

## 8. Remediation (review findings; do not untick 1.1–7.2)

- [x] 8.1 W1a exclusive: `packages/core/src/types.ts` and `packages/core/src/parse-config.test.ts` only. `FeedOptionsSchema` superRefine: `new URL(url)` username and password MUST be empty. Add a userinfo parse-fail test alongside the existing http url test. MUST NOT replace `FIRST_PARTY_PLUGIN_IDS` with github+rss only; ids remain `github`, `wakatime`, `rss`, `http`.
- [x] 8.2 W1b exclusive: `packages/integrations/src/rss/ssrf.ts`. `isSSRFSafeURL` with `allowedProtocols: ["https"]`, `allowUsername: false`, `autoPrependProtocol: false`. `pinnedHttpsGet` option bag only (never `https.request(url, ...)`). Drain 3xx bodies before `Location`. One `AbortSignal.timeout(RSS_TIMEOUT_MS)` for the whole hop loop. Remove the abort listener in `finally`. Keep `RSS_MAX_REDIRECTS = 5`. Reuse `ssrfcheck` already in integrations `package.json`. FORBIDDEN: http pack rewrite, extra undici.
- [x] 8.3 W1d exclusive: `packages/integrations/src/rss/client.ts`. `parseFeedUrl` rejects userinfo with `RssClientError("fail_widget")`. Never send `Authorization`.
- [x] 8.4 W1c exclusive: `packages/integrations/src/rss/ssrf.test.ts` after 8.2. Pin-path (omit `fetch`; mock `node:https.request`; lookup returns the validated address set; `servername === hostname`). Userinfo. ULA `fd00:ec2::254`, CGNAT `100.64.0.0/10`, reserved `2001:db8::1`, localhost, decimal `2130706433`. Redirect `Location: https://github.com/...`. Oversize redirect body cancelled.
- [x] 8.5 W1e exclusive: `packages/integrations/src/rss/client.test.ts` after 8.3. Userinfo `fail_widget`. Retry-After cap. XXE response MUST NOT contain passwd file contents.
- [x] 8.6 W2a exclusive: `packages/action/src/engine.ts` and `engine.test.ts`. `EnabledWidget` += `{ id: "feed"; options: FeedOptions }`. `enabledWidgets` appends feed after github widgets. `preflightWidget`: `demo` and `feed` → `"render"` (no `include_private`). `failedWidget` only for github-integration widgets. Rss `fail_widget` → no write, no throw at end. Rss-only 404: job succeeds with no files. `decideAllGithubWidgetsSkipped` unchanged. Tests: rss yaml enables feed; rss `fail_widget` plus successful github skip/render does not throw; preflight feed does not read `include_private`.
- [x] 8.7 W2b after 8.2 and 8.6. Exclusive: `packages/action/src/render-feed.ts`, `render-feed.test.ts`, `packages/action/src/main.ts`, `packages/action/package.json`, `packages/action/AGENTS.md`. `createRssClient()` once per `runEngine`; catch `RssClientError` → `{ outcome: "fail_widget" }`. Dispatcher `id === "feed"` uses that renderer. Add workspace deps `@profile-bits/core`, `@profile-bits/integrations`, `@profile-bits/plugins`, `@profile-bits/renderer` if missing (`pnpm install` once if the lockfile changes). Rss-only yaml plus mocked lookup/https writes 480×160 svg with no `Authorization`. FORBIDDEN: `flatten.ts`, `DEFAULT_YAML`, github crawl.
- [x] 8.8 W3 exclusive: `apps/docs/src/preview/server/rss-fixtures.ts`, `rss-fixtures.test.ts`, and the rss fixture wrap sentence in `apps/docs/AGENTS.md`. `parseRssXml(loadFixture(...))` plus `renderFeedSvg`. Zero live network. Atom fixture renders titles; empty fixture → “No feed items”. MUST NOT change `PREVIEW_WIDGET_IDS`. MUST NOT add `/playground/rss`. MUST NOT add `data-group="feed"`. MUST NOT call `createRssClient({ fetch })`.
- [x] 8.9 W4 after 8.1–8.8. Focused vitest on the exclusive test files above. Tick 8.1–8.9 only. Biome only on touched paths if full `just lint` is dirty from other waves. Do not commit. Do not archive. Do not untick 1.1–7.2.
