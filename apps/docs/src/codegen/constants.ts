import type { PreviewPluginId } from "@/src/preview/types";

/** v0 first-party pack. Safe for server pages — do not import from client shells. */
export const PLAYGROUND_PLUGIN: PreviewPluginId = "github";
